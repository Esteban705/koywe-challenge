meta {
  name: create-quote
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}quote
  body: json
  auth: bearer
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "amount": 1000,
    "from": "USD",
    "to": "EUR",
    "currency": "USD"
  }
}

tests {
  test("should return 201 status code", () => {
    expect(res.status).to.equal(201);
  });
} 